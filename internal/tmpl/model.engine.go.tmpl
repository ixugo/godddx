// Code generated by godddx.
package {{.PackageName}}

import "github.com/ixugo/goddd/pkg/orm"

// {{.Name | ToUpperCamelCase}} domain model
type {{.Name | ToUpperCamelCase}} struct{
	{{range .Lines}}
	{{.Name}} {{.Type}} `{{.Tag}} json:"{{.Name | ToUnderscore}}"` {{.Comment|ToComment}}{{end}}
}

// TableName database table name
func (*{{.Name}}) TableName() string {
	return "{{.Name | Plural}}"
}

// CacheKey 缓存主键，必须唯一
// godddx 生成缓存代码时，依赖的主键
// 默认应该是 ID 字段，但也可以自定义
func ({{.Name | FirstLetter}} *{{.Name}}) CacheKey() string {
	{{if .IsStringID}}
	return {{.Name | FirstLetter}}.ID
	{{else}}
	return fmt.Sprintf("%d", {{.Name | FirstLetter}}.ID)
	{{end}}
}
